# 统一按 LF 存入仓库，避免 Windows 上 CRLF 转换导致工作区反复变脏。
# 注意：.cmd/.bat 必须保留 CRLF，否则部分 Windows 环境下无法正常执行。
* text=auto eol=lf

*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# 二进制文件不做行尾转换
*.png binary
*.ico binary
*.exe binary
*.zip binary
